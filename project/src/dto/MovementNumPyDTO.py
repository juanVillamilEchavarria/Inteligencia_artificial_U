class MovementNumPyDTO:
    def __init__(self, median, average, std, min, max, count, sum):
        self.median = median
        self.average = average
        self.std = std
        self.min = min
        self.max = max
        self.count = count
        self.sum = sum